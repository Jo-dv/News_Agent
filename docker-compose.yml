services:
  agent_app:
    build: .
    container_name: agent_app
    volumes:
      - .:/app
      - ./chroma_data:/app/chroma_data  # ChromaDB 데이터 영구 보존
    env_file:
      - .env
    restart: unless-stopped